// Package audiobooks owns silo's first-party audiobook + podcast feature,
// absorbed from the historical silo-plugin-audiobooks. It covers the
// ABS-compat REST surface, Socket.io, scanner branches, podcast feed
// refresh, and the silo SPA pages, gated behind the kill-switch reader.
package audiobooks
